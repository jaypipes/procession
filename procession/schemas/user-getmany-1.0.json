{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "title": "A user within the Procession deployment.",
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "integer",
            "description": "Identifier for the user."
        },
        "name": {
            "type": "string",
            "description": "Short name for the user (used in determining the user's 'slug' value)."
        },
        "slug": {
            "type": "string",
            "description": "Lowercased, hyphenated non-UUID identififer used in URIs."
        },
        "email": {
            "type": "string",
            "description": "Email address to use for the user.",
            "format": "email"
        },
        "createdOn": {
            "type": "string",
            "description": "The date-time when the organization was created, in ISO 8601 format.",
            "format": "date-time"
        }
    }
}
