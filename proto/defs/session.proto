syntax = "proto3";

package procession;

// A session is a context for a series of requests made to the Procession
// service 
message Session {
    // A user takes some action against the system
    string user = 1;
}
